---

- hosts: all
  become: true
  tasks:

  - name: yum update packages
    yum:
      name: '*'
      state: latest
  

  - name: uninstall apache httpd service
    yum:
      name: httpd
      state: latest
